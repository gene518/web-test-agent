"""Healer 模式使用的移动端修复约定。"""

MOBILE_UI_CONVENTIONS_PROMPT = """\
# Healer 阶段移动端修复约定
本约定只补充 `healer.py` 之外的执行细则；`healer.py` 已明确的角色定位、调试主流程和总体修复原则，这里不再重复。

## 阶段边界

- 输入是已经存在且需要调试的一个或多个 `.spec.ts` 脚本文件，不是测试计划。
- Healer 阶段的主要产出是通过验证的脚本修复；不要把任务改造成重新生成功能或重新设计测试计划。
- 修复范围以 `test_scripts` 为主；只有在共享基础代码直接导致这些脚本失败时，才修改 `test_case/shared/` 等被这些脚本实际依赖的文件。

## 运行与验证范围

- `test_run` 和 `test_debug` 应优先只针对 `test_scripts` 中给出的脚本路径执行，不要默认跑全量工程。
- 如果多个脚本同时失败，按 `test_scripts` 给出的顺序逐个处理，先收敛单个失败原因，再继续下一个。
- 每次代码修改后都要重新运行相关脚本验证，不要只看静态代码就宣布修复完成。

## 移动端交互修复规则

- 按移动端真实交互语义理解原脚本：点击操作优先使用 `tap()`，不要把移动端关键交互随意退回成 `click()`。
- 发送文本时优先通过页面发送按钮提交，不要用 Enter 代替真实发送动作，除非页面交互本身就是键盘提交。
- 同一种交互方式最多连续尝试两次；仍失败时应先重新观察页面状态，再改定位器、等待或操作方式。
- 所有修复都应继续模拟真实用户行为；不要用 `page.evaluate()` 直接触发业务 DOM 行为来“修复”脚本。

## 代码修复规则

- 优先修复失效定位器、错误断言、时序问题和共享辅助方法中的真实缺陷。
- 若因动态文案、动态数字或波动数据导致失败，优先改成更稳健的匹配方式，不要写死脆弱值。
- 若因页面加载或渲染时机导致失败，可使用显式等待；需要兜底时优先考虑短时 `page.waitForTimeout(...)`，并保持最小必要时长。
- 不要删除关键断言来换取通过；如果确有必要调整断言，必须保留原业务意图。

## 计划同步约定

- 如果修复过程中确认原脚本对应的测试计划步骤或预期已经不准确，且项目中存在对应 `aaa_*.md` 计划文件，可补充 `[UPDATED]` 记录。
- 不要直接覆写原计划场景；只追加经验证后的 `[UPDATED]` 版本，供后续人工确认。

## 收尾标准

- 只有当 `test_scripts` 涉及的失败项重新运行通过，或已按规则明确落成 `test.fixme()`，本次修复才算完成。
- 最终说明中应清楚交代失败根因、修改点和验证结果。
"""
