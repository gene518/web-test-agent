# Web AutoTest Agent 客户使用指南

Web AutoTest Agent 用于把 Web 测试需求转成测试计划、Playwright 自动化脚本和可验证的测试结果。你可以用一句自然语言完成完整流程，也可以分阶段确认后再继续。

本文只介绍日常使用方法。部署、模型配置和二次开发请参考项目根目录的 `README.md` 与 `DEVELOPMENT_GUIDE.md`。

## 1. 开始使用

### 1.1 使用前准备

开始前，建议准备以下信息：

- 被测环境 URL，例如 `https://test.example.com/login`。
- 一个固定且容易识别的工程名，例如 `mall-smoke`。同一项目后续继续使用相同名称。
- 本次要覆盖的功能、优先级和用例数量。
- 可重复使用的测试账号和测试数据。不要在对话中发送生产密码、个人隐私或其他敏感信息。
- 如果要生成或调试已有资产，准备好项目内测试计划或 `.spec.ts` 脚本的路径。

推荐优先使用测试环境。目标页面需要能从运行 Web AutoTest Agent 的机器访问；验证码、短信验证、硬件密钥和部分单点登录流程可能需要人工配合或预先准备登录状态。

### 1.2 打开工具

- 已部署版本：打开管理员提供的访问地址。
- 本地 macOS 版本：双击 `start/macos-start.command`，服务就绪后通常会自动打开页面。
- 本地 Windows 版本：右键 `start\windows-start.ps1`，选择“使用 PowerShell 运行”。
- 浏览器没有自动打开时，访问 `http://localhost:3000`。

进入页面后，点击“新建对话”，在输入框中描述任务。执行期间可以查看页面探索、工具调用和阶段结果；系统追问信息时，直接在当前补充框中回答即可。

## 2. 一句话写清任务

一个高质量请求通常包含六部分：

```text
要做什么 + 工程名 + 目标 URL/输入文件 + 测试范围 + 数量或优先级 + 完成标准
```

推荐模板：

```text
请为【目标 URL】的【功能范围】生成【数量】条【优先级】测试用例，
工程名为【工程名】；然后生成 Playwright 脚本并运行调试，直到相关脚本通过。
约束：【账号/数据/禁止操作/其他要求】。
```

例如：

```text
请为 https://test.example.com/login 的账号密码登录功能生成 3 条最高优先级测试用例，
工程名为 mall-smoke；然后生成 Playwright 脚本并运行调试，直到这 3 条脚本通过。
只使用测试账号，不执行注册、充值或删除数据操作。
```

相比“帮我测一下这个网站”，明确范围、数量、优先级和完成标准能显著减少追问，也更容易得到稳定结果。

## 3. 选择合适的使用方式

| 你的目标                 | 推荐方式     | 必要信息                   | 主要结果                   |
| ------------------------ | ------------ | -------------------------- | -------------------------- |
| 先分析应该测什么         | 生成测试计划 | 工程名、URL                | Markdown 测试计划          |
| 已有计划，需要自动化代码 | 生成脚本     | 工程名或工程目录、计划路径 | Playwright `.spec.ts` 脚本 |
| 已有脚本运行失败         | 调试修复     | 工程名或工程目录、脚本路径 | 修复后的脚本与验证结果     |
| 从需求直接做到可运行脚本 | 完整流程     | 工程名、URL、测试范围      | 计划、脚本、调试结果       |
| 周期执行已有脚本         | 定时任务     | 工程名或工程目录、执行时间 | 定时任务配置               |
| 咨询测试方法             | 测试问答     | 具体问题和上下文           | 测试建议                   |

两种工作方式的选择建议：

- 快速冒烟、需求明确、用例较少：使用一句话完整流程。
- 核心业务、复杂流程、发布前回归：先生成计划并人工确认，再生成脚本，最后调试。
- 已有计划或脚本：直接从“生成脚本”或“调试修复”开始，不必重新生成计划。

## 4. 最佳实践案例

以下示例可以直接替换工程名、URL、功能点和路径后使用。

### 案例一：发布前快速冒烟

适合需求明确、需要尽快得到一组可运行脚本的场景。

```text
请为 https://test.example.com 的登录、商品搜索和退出登录生成 3 条最高优先级冒烟用例，
工程名为 release-smoke；按“测试计划 -> 生成脚本 -> 调试验证”的顺序连续执行，
直到这 3 条脚本通过。不要提交订单或修改用户资料。
```

建议先控制在 3 至 5 条高优先级用例。小批量完成并确认后，再补充下一批，可以降低页面变化或测试数据相互影响带来的失败。

### 案例二：先评审计划，再生成脚本

适合支付、权限、订单等关键业务。第一轮只生成计划：

```text
请为 https://test.example.com/orders 的订单查询功能生成测试计划，工程名为 order-regression。
重点覆盖按订单号查询、状态筛选、空结果和翻页，按 P0/P1/P2 标注优先级。
本轮只生成测试计划，不生成脚本。
```

确认系统返回的用例名称、步骤、预期结果和计划文件路径。需要调整时，在同一对话中回复：

```text
请更新刚才的计划：删除导出场景，补充“清空筛选条件后恢复默认列表”，
并把“按订单号精确查询”设为 P0。
```

计划确认后继续回复：

```text
计划已确认。请基于当前测试计划生成全部 Playwright 脚本。
```

脚本生成后继续回复：

```text
请运行并调试刚生成的脚本，修复测试脚本问题并重新验证，直到相关脚本通过。
```

在同一对话中继续操作，系统会优先承接刚生成的计划和脚本。

### 案例三：从已有计划生成指定脚本

适合已有测试资产，只自动化本次需要的用例。

```text
工程名为 order-regression。
请读取测试计划 test_case/order_search/aaa_order_search.md，
只生成“按订单号精确查询”和“无匹配结果”两条用例的 Playwright 脚本。
```

也可以提供计划目录，系统会在项目范围内查找可用的 Markdown 计划文件：

```text
工程目录为 /Users/tester/webautotest/order-regression，
请根据 test_case/order_search 目录中的测试计划生成全部脚本。
```

计划文件必须位于指定自动化工程内。路径不确定时，可以先从上一阶段结果中复制系统返回的相对路径。

### 案例四：修复已有失败脚本

适合页面元素、等待条件或断言变化导致的自动化脚本失败。

```text
工程名为 order-regression。
请运行并修复脚本 test_case/order_search/search_by_order_id.spec.ts。
当前现象是点击“查询”后等待结果超时；请定位原因，只修改当前工程内的测试代码，
修复后重新运行该脚本并给出验证结果。
```

批量修复时可以提供目录：

```text
工程目录为 /Users/tester/webautotest/order-regression，
请调试 test_case/order_search 目录下的全部 .spec.ts 脚本，
逐个修复并复测，不要扩大到其他测试目录。
```

错误现象、失败步骤和最近的页面改动越具体，定位通常越快。修复阶段处理的是测试脚本；如果失败来自真实业务缺陷，系统不会修改被测业务系统来让测试通过。

### 案例五：配置工作日定时回归

适合脚本已经稳定通过，需要周期运行的场景。

```text
请把工程 /Users/tester/webautotest/order-regression 设置为每个工作日 09:00 无头执行，
只运行 test_case/order_search 目录，并启用定时任务。
```

更新任务时继续提供同一工程：

```text
请把 order-regression 的定时任务改为每天 20:30 执行，并保持无头模式。
```

暂停任务：

```text
请把 order-regression 每天 20:30 的定时任务设为禁用，其他配置保持不变。
```

定时任务配置成功不代表测试已经立即执行。独立调度服务必须处于运行状态；未单独配置项目时区时，执行时间使用调度服务所在机器的本地时区。定时执行失败后不会自动调用脚本修复功能。

### 案例六：让工具提供测试建议

不需要生成文件时，可以直接咨询测试问题：

```text
我们把登录接口从密码认证改成短信验证码认证。请列出前端回归测试需要关注的风险，
按功能、兼容性、安全性和可用性分类，并标出发布前必须完成的项目。
```

这类问答不会自动进入测试计划、脚本生成或调试阶段。需要落地资产时，请明确使用“生成测试计划”或“生成脚本”等表达。

## 5. 什么时候使用哪种模式

| 场景或时机             | 建议                                               |
| ---------------------- | -------------------------------------------------- |
| 新功能刚进入可测试状态 | 先生成计划，确认范围和测试数据后再写脚本           |
| 每日构建或发布前       | 运行 3 至 10 条稳定的 P0 冒烟脚本                  |
| 核心流程大改           | 分阶段执行，先评审计划，不要直接批量生成全部脚本   |
| 页面只有文案或样式小改 | 直接运行现有脚本，失败时再进入调试修复             |
| 用例数量较多           | 按功能模块或优先级分批处理，避免一次提交过大范围   |
| 生产环境               | 原则上只做只读验证；优先改用隔离的测试或预发布环境 |
| 定时回归               | 先手动确认脚本稳定通过，再启用无头定时执行         |

## 6. 如何检查结果

不要只看最后一句“完成”，还要核对以下内容。

### 测试计划阶段

- 是否明确显示“已保存测试计划”及文件路径。
- 用例是否覆盖你指定的功能点、优先级和数量。
- 每条用例是否包含前置条件、操作步骤和可验证的预期结果。
- “待生成脚本规划”只是后续目标，不代表脚本已经生成。

### 脚本生成阶段

- “已生成脚本”的数量是否与选定用例一致。
- 每个 `.spec.ts` 路径是否位于当前自动化工程内。
- 生成成功不等于运行通过；需要继续执行调试验证。

### 调试修复阶段

- 调试目标是否只包含你指定的脚本或目录。
- 是否列出实际变更文件和重新运行的验证目标。
- 是否给出通过、失败或跳过结果；仍失败时应查看具体原因，而不是反复要求无条件通过。
- 需要查看 HTML 报告时，可在自动化工程中使用 Playwright 的报告命令，或联系项目管理员获取报告位置。

### 定时任务阶段

- 工程、执行时间、脚本范围、有头/无头和启用状态是否正确。
- 确认调度服务正在运行，并确认它使用的时区。
- 定时执行日志默认位于项目的 `test_case/scheduler-service.log`。

## 7. 提高成功率的建议

1. 一个工程使用一个稳定名称。同一对话尽量只处理同一工程和同一业务主题。
2. 把“登录页”改成具体 URL，把“主要功能”改成可观察的功能点。
3. 明确数量和优先级，例如“只生成 3 条 P0 用例”，不要默认让系统覆盖整个站点。
4. 写清禁止操作，例如“不发送真实短信、不支付、不删除数据”。
5. 为自动化准备固定账号、独立数据和可恢复的初始状态，避免用例之间互相污染。
6. 页面元素尽量提供稳定的可访问名称或 `data-testid`，减少依赖易变的 CSS 层级。
7. 先小批量完成计划、生成和调试，再扩展回归范围。
8. 在同一对话中承接上一步产物；切换工程或完全不同的任务时新建对话。
9. 页面或需求变化后先更新测试计划，再同步更新和验证对应脚本。
10. 对关键业务结果进行人工复核。自动生成结果应进入团队正常的代码评审和测试资产管理流程。

## 8. 常见问题

### 系统一直追问工程名、URL 或文件路径

说明当前阶段缺少必要信息。在当前补充框中直接给出被询问的值即可，不需要重新描述整个任务。生成计划至少需要工程名和 URL；生成脚本需要工程信息和测试计划路径；调试需要工程信息和脚本路径。

### 系统打不开目标页面

先确认运行工具的机器能够访问目标 URL，并检查 VPN、内网、代理、证书和防火墙。页面依赖验证码、短信或单点登录时，改用可自动化的测试入口或预先准备登录状态。

### 为什么生成了脚本但没有测试通过结果

生成与调试是两个阶段。继续回复“请运行并调试刚生成的脚本，直到相关脚本通过”，或在最初请求中明确要求执行完整流程。

### 新对话为什么没有继续使用上次的计划

自动承接优先发生在同一对话内。新建对话后，请明确提供工程名或工程目录，以及计划或脚本路径。

### 定时任务配置成功后为什么没有执行

对话只创建或更新配置，实际执行依赖独立调度服务。请确认服务正在运行、任务已启用、Cron 时间命中正确时区，并查看 `test_case/scheduler-service.log`。

### 修复后仍有失败怎么办

查看失败属于脚本问题、环境问题、测试数据问题还是业务缺陷。补充错误信息并缩小到单个脚本再次调试。验证码、第三方服务异常、网络不稳定或真实产品缺陷通常不能通过修改测试脚本可靠解决。

## 9. 当前能力边界

- 工具生成和修复的是自动化测试资产，不会修复被测业务系统。
- 不能保证所有动态页面、验证码、复杂单点登录或第三方系统都能完全自动化。
- 计划和脚本只能在当前自动化工程允许的目录范围内读取或写入。
- 定时任务按队列串行执行；高频任务或长任务可能排队。
- 定时执行失败不会自动触发脚本修复。
- 自动生成的计划、脚本和结果仍需要业务或测试人员审核，尤其是支付、权限、数据删除等高风险场景。
